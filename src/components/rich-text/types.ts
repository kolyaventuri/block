export type RichTextStyle = {
  bold?: boolean;
  italic?: boolean;
  strike?: boolean;
  code?: boolean;
};

export type RichTextListStyle = 'ordered' | 'bullet';

export type RichTextBroadcastRange = 'here' | 'channel' | 'everyone';
